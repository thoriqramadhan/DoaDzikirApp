<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DoaDzikirApp" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorSurface">@color/secondargGreen</item>
        <item name="colorOnSurface">@color/white</item>
        <item name="android:statusBarColor">@color/mainGreen</item>

        <item name="fontFamily">@font/roboto_black</item>
    </style>

    <style name="Theme.DoaDzikirApp" parent="Base.Theme.DoaDzikirApp" />
    <style name="Theme.App.DzikirPagi" parent="Base.Theme.DoaDzikirApp">
        <item name="android:statusBarColor">@color/mainGreen</item>
        <item name="colorSurface">@color/secondargGreen</item>
    </style>
    <style name="Theme.App.DzikirPetang" parent="Base.Theme.DoaDzikirApp">
        <item name="android:statusBarColor">@color/afternoon_orange</item>
        <item name="colorSurface">@color/afternoon_orange</item>
    </style>
</resources>